<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.NBAStables">

        <!-- Scores Widget -->
        <receiver
            android:name=".ScoresWidgetProvider"
            android:exported="true"
            android:label="NBA Scores">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/scores_widget_info" />
        </receiver>

        <!-- Injuries Widget -->
        <receiver
            android:name=".InjuriesWidgetProvider"
            android:exported="true"
            android:label="NBA Injuries">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/injuries_widget_info" />
        </receiver>

        <!-- Standings Widget -->
        <receiver
            android:name=".StandingsWidgetProvider"
            android:exported="true"
            android:label="NBA Standings">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/standings_widget_info" />
        </receiver>

    </application>

</manifest>
