# Deps for api/static/update_injuries.py only — runs in GitHub Actions, never in the
# runtime image. Kept out of requirements.txt so lxml/bs4 stay out of the Docker build.
requests==2.34.2
beautifulsoup4==4.15.0
lxml==6.1.1
